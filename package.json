{
  "name": "sunlight-x",
  "version": "0.0.1",
  "description": "Sunlight highlighter refactored for Node.js",
  "main": "babel-out/index.js",
  "author": "WC Leung",
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-eslint": "^7.2.3",
    "babel-plugin-tcomb": "^0.3.26",
    "babel-preset-env": "^1.6.0",
    "babel-preset-flow": "^6.23.0",
    "cross-env": "^5.0.1",
    "eslint": "^4.2.0",
    "eslint-config-prettier": "^2.3.0",
    "eslint-plugin-flowtype": "^2.35.0",
    "eslint-plugin-prettier": "^2.1.2",
    "flow-bin": "^0.51.0",
    "flow-typed": "^2.1.5",
    "jest": "^20.0.4",
    "less": "^2.7.2",
    "prettier": "^1.5.3",
    "tcomb": "^3.2.21"
  },
  "dependencies": {
    "jsdom": "^9.12.0",
    "winston": "^2.3.1"
  },
  "jest": {
    "testMatch": [
      "**/test/**/*.js?(x)"
    ],
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/fixtures/",
      "/code-snippets/"
    ]
  },
  "scripts": {
    "babel": "babel",
    "compile": "yarn run compile-js && yarn run compile-css",
    "compile-css": "lessc style/sunlight.less compiled-assets/sunlight.css && lessc style/sunlight-defaultfont.less compiled-assets/sunlight-defaultfont.css",
    "compile-debug": "yarn run compile-js-debug && yarn run compile-css",
    "compile-js": "babel -d babel-out/ src/",
    "compile-js-debug": "cross-env NODE_ENV=test babel -d babel-out/ src/",
    "eslint": "eslint",
    "eslint-check": "eslint --print-config .eslintrc | eslint-config-prettier-check",
    "eslint-run": "eslint .",
    "flow": "flow",
    "flow-typed": "flow-typed",
    "flow-typed-install": "flow-typed install",
    "jest": "jest",
    "lessc": "lessc",
    "lint": "yarn run flow-typed-install && yarn run eslint-run",
    "prepare": "yarn run compile",
    "test": "yarn run lint && yarn run testOnly",
    "testOnly": "yarn run jest"
  }
}
